#include <iostream>
using namespace std;

int main() {
	int a = 2, b = 3;

	//a == b equality check: is a equal to b?
	//a != b not equal

	// a < b
	// a > b
	// a <= b
	// a >= b
	//ILLEGAL:  a <> b      a =< b


	//	precedence

	if (a == 2 && b == 3) {

	}

	if (a + 3 < b)  // precedence is what you would expect: ((a+3) < b)
	
	return 0;
}
